<UserControl x:Class="ConfigAdmin.Wpf.Views.RemoteNodesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Text="RDP-узлы" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,8" />
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="Добавить" Command="{Binding AddCommand}" Margin="0,0,8,0" Padding="10,4" />
                <Button Content="Редактировать" Command="{Binding EditCommand}" Margin="0,0,8,0" Padding="10,4" />
                <Button Content="Копировать Node ID" Command="{Binding CopyNodeIdCommand}" Margin="0,0,8,0" Padding="10,4" />
                <Button Content="Обновить" Command="{Binding RefreshCommand}" Padding="10,4" />
            </StackPanel>
            <TextBlock Text="{Binding ReceiverStatus}" Foreground="Gray" Margin="0,0,0,4" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="Firebrick" TextWrapping="Wrap" />
        </StackPanel>

        <DataGrid ItemsSource="{Binding Nodes}"
                  SelectedItem="{Binding SelectedNode}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Клиент" Binding="{Binding ClientName}" Width="140" />
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="160" />
                <DataGridTextColumn Header="Node ID" Binding="{Binding NodeIdText}" Width="*" />
                <DataGridTextColumn Header="Статус" Binding="{Binding OnlineStatus}" Width="80" />
                <DataGridTextColumn Header="Last seen" Binding="{Binding LastSeenText}" Width="160" />
                <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding Enabled}" Width="70" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
